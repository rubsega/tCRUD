@model List<Producto>

<h1>Productos</h1>

<a asp-action="Create" class="btn btn-primary mb-3">Nuevo Producto</a>

@if (Model.Any()) {
<table class="table table-stripeda">
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Precio</th>
            <th>Stock</th>
            <th>Categoria</th>
            <th>Descripcion</th>
            <th style="width: 1%"></th>

        </tr>
    </thead>
    <tbody>
        @foreach (var p in Model){
            <tr>
                <td>@p.Nombre</td>
                <td>@p.Precio.ToString("C")</td>
                <td>@p.Stock</td>
                <td>@p.Categoria</td>
                <td>@p.Descripcion</td>
                <td>
                    <div class="d-flex gap-1">
                        <a asp-action="Edit" asp-route-id="@p.Id" class="btn btn-sm btn-warning">Editar</a>
                        <button type="button" class="btn btn-sm btn-danger"
                                data-bs-toggle="modal" data-bs-target="#modalEliminar"
                                data-id="@p.Id" data-nombre="@p.Nombre">
                            Eliminar
                        </button>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>

<div class="modal fade" id="modalEliminar" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Confirmar eliminación</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                ¿Seguro que quieres eliminar <strong id="modalNombreProducto"></strong>?
                Esta acción no se puede deshacer.
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                <form id="formEliminar" method="post">
                    @Html.AntiForgeryToken()
                    <button type="submit" class="btn btn-danger">Eliminar</button>
                </form>
            </div>
        </div>
    </div>
</div>

} else { <p>No hay productos todavía.</p> }

@section Scripts {
    <script>
        const modal = document.getElementById('modalEliminar');
console.log('modal encontrado:', modal);   // ¿null o el elemento?
        modal.addEventListener('show.bs.modal', function (event) {
            // el botón que disparó el modal
            const boton = event.relatedTarget;

            // leerle su carga de datos
            const id = boton.getAttribute('data-id');
            const nombre = boton.getAttribute('data-nombre');

            // rellenar los huecos
            document.getElementById('modalNombreProducto').textContent = nombre;
            document.getElementById('formEliminar').action = '/Productos/Delete/' + id;
        });
    </script>
}